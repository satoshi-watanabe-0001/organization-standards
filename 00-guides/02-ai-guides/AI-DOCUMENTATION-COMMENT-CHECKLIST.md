---
title: "AI Documentation Comment Checklist (Multi-Language)"
version: "1.0.0"
created_date: "2025-01-06"
status: "Active"
owner: "Engineering Team"
---

# AI向けドキュメントコメントチェックリスト（全言語対応）

> Phase 3実装時に必ず参照するチェックリスト（言語非依存）

---

## 🌐 対象言語

このチェックリストは以下の言語に適用されます:
- ✅ TypeScript/JavaScript (JSDoc)
- ✅ Python (Docstring)
- ✅ Java (Javadoc)
- ✅ その他（各言語標準に準拠）

---

## ✅ ファイルレベル（全言語共通）

### ファイルヘッダーコメント
- [ ] ファイルの目的を1行で記述した
- [ ] ファイルの詳細説明を2-3行で記述した
- [ ] モジュール/パッケージ構造を明記した
- [ ] 必要に応じて著作権・ライセンスを記載した

### 言語別確認

| 言語 | 形式 | 必須タグ |
|------|------|---------|
| TypeScript/JS | JSDoc | `@fileoverview`, `@module` |
| Python | Docstring | モジュールDocstring（ファイル先頭） |
| Java | Javadoc | パッケージJavadoc（package-info.java） |

**確認方法**:
```bash
# TypeScript/JavaScript
grep -r "@fileoverview" src/

# Python
grep -r '"""' src/*.py | head -1

# Java
find src -name "package-info.java"
```

---

## ✅ クラス・インターフェースレベル（全言語共通）

### パブリッククラス
- [ ] クラスの目的を1行で記述した
- [ ] クラスの責任を詳細に説明した
- [ ] 使用例を記述した（複雑なクラスの場合）
- [ ] 設計パターンを明記した（該当する場合）
- [ ] スレッドセーフ性を明記した（並行処理がある場合）

### パブリックインターフェース
- [ ] インターフェースの目的を記述した
- [ ] すべてのプロパティ/メソッドに説明を記述した

### 言語別確認

**TypeScript/JavaScript**:
```typescript
/**
 * [クラスの目的]
 * 
 * [詳細説明]
 * 
 * @class
 * @example
 * ```typescript
 * const instance = new ClassName();
 * ```
 */
export class ClassName {
  /** [プロパティの説明] */
  propertyName: Type;
}
```

**Python**:
```python
class ClassName:
    """クラスの目的
    
    詳細説明
    
    Attributes:
        property_name: プロパティの説明
    
    Example:
        >>> instance = ClassName()
        >>> instance.do_something()
    """
    pass
```

**Java**:
```java
/**
 * クラスの目的
 * 
 * <p>詳細説明</p>
 * 
 * @author 作成者名
 * @since 1.0.0
 * @see 関連クラス
 */
public class ClassName {
    /** プロパティの説明 */
    private String propertyName;
}
```

---

## ✅ 関数・メソッドレベル（全言語共通）

### すべてのパブリック関数/メソッド
- [ ] 関数の目的を1行で記述した
- [ ] 関数の詳細動作を説明した
- [ ] すべてのパラメータに説明を記述した
- [ ] 戻り値の説明を記述した
- [ ] 発生しうる例外をすべて記述した
- [ ] 使用例を記述した（複雑な関数の場合）
- [ ] 副作用を明記した（状態変更がある場合）
- [ ] 時間計算量を記載した（パフォーマンス重視の場合）

### 言語別必須タグ

| 言語 | パラメータ | 戻り値 | 例外 |
|------|----------|--------|------|
| TypeScript/JS | `@param` | `@returns` | `@throws` |
| Python | `Args:` | `Returns:` | `Raises:` |
| Java | `@param` | `@return` | `@throws` |

---

## ✅ コード内コメントレベル（全言語共通）

### 複雑なロジック（循環的複雑度10以上）
- [ ] ロジックの意図を説明した
- [ ] ビジネスルール・制約を明記した
- [ ] なぜその実装になったかを説明した
- [ ] アルゴリズムの時間計算量を記載した（該当する場合）

### 非自明な実装
- [ ] パフォーマンス最適化の理由を説明した
- [ ] 技術的回避策の背景を説明した
- [ ] セキュリティ上の考慮事項を記載した
- [ ] 将来の改善点を記載した（TODO/FIXME）

**良い例（言語非依存の原則）**:
```
# なぜそうするのか（Why）を説明
# ビジネスルール: ○○の理由により××を実装
# パフォーマンス考慮: ○○により△△倍高速化
# セキュリティ: ○○攻撃を防ぐため××を実装
```

---

## ✅ 実装完了後の最終チェック（言語別）

### TypeScript/JavaScript
```bash
- [ ] npm run lint が成功する
- [ ] npm run lint:jsdoc が成功する
- [ ] npm run type-check が成功する
- [ ] npm run docs:generate（TypeDoc）が成功する
```

### Python
```bash
- [ ] pylint --enable=missing-docstring src/ が成功する
- [ ] pydocstyle src/ が成功する
- [ ] mypy src/ が成功する
- [ ] sphinx-build（オプション）が成功する
```

### Java
```bash
- [ ] mvn checkstyle:check が成功する
- [ ] mvn javadoc:javadoc が成功する
- [ ] mvn pmd:check が成功する
```

---

## 🚨 よくあるエラーと対処法（言語横断）

### エラー1: ドキュメントコメントがない

**TypeScript/JavaScript**:
```
error: Missing JSDoc comment (jsdoc/require-jsdoc)
```

**Python**:
```
error: Missing module docstring (C0114)
error: Missing function docstring (C0116)
```

**Java**:
```
error: Missing a Javadoc comment (JavadocMethod)
```

**対処**: 言語別テンプレートを使用してコメントを追加

---

### エラー2: パラメータの説明が不足

**TypeScript/JavaScript**:
```
error: Missing JSDoc @param (jsdoc/require-param)
```

**Python**:
```
warning: Missing parameter(s) in Docstring: userId (D417)
```

**Java**:
```
error: @param tag missing for parameter userId (JavadocMethod)
```

**対処**: すべてのパラメータに説明を追加

---

### エラー3: 戻り値の説明が不足

**TypeScript/JavaScript**:
```
error: Missing JSDoc @returns (jsdoc/require-returns)
```

**Python**:
```
warning: Missing return type in Docstring (D400)
```

**Java**:
```
error: @return tag missing (JavadocMethod)
```

**対処**: 戻り値の詳細説明を追加

---

## 📊 品質基準（言語非依存）

### ドキュメントコメントカバレッジ目標
- **パブリックAPI**: 100%（必須） 🔴
- **複雑なロジック**: 80%以上（推奨） 🟡
- **プライベートメソッド**: 50%以上（任意） ⚪

### コメント品質基準
- **What（何を）**: 30%
- **Why（なぜ）**: 70% ← **最重要**

### 循環的複雑度基準
- **10以上**: コメント必須 🔴
- **20以上**: リファクタリング検討 ⚠️

---

## 🔗 参照ドキュメント（言語別）

### TypeScript/JavaScript
- 🔴 `/01-coding-standards/typescript-javascript-standards.md` - セクションX
- 🔴 `/08-templates/code-templates/typescript/`

### Python
- 🔴 `/01-coding-standards/python-standards.md` - セクションX
- 🔴 `/08-templates/code-templates/python/`

### Java
- 🔴 `/01-coding-standards/java-standards.md` - セクションX
- 🔴 `/08-templates/code-templates/java/`

### 言語非依存
- 🔴 `/03-development-process/documentation-standards.md` - セクション7
- 🔴 `/00-guides/phase-guides/phase-3-implementation-guide.md` - セクション3.7
- 🟡 `/09-reference/best-practices.md` - ドキュメントコメントセクション

---
