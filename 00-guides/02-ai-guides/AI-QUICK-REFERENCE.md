# AI開発者向け クイックリファレンス

**対象**: Devin、Cursor、GitHub Copilot、その他のAI開発支援ツール  
**最終更新**: 2025-10-23

---

## 🚀 コード生成前の必須チェック

```
□ 使用言語の規約ファイルを確認したか？
□ 共通原則（00-general-principles.md）を確認したか？
□ 数値基準（関数サイズ、複雑度等）を把握したか？
□ ドキュメンテーション形式（JSDoc、Docstring等）を確認したか？
```

---

## 📏 必須遵守の数値基準

| メトリクス | 理想 | 推奨上限 | 絶対上限 |
|-----------|------|----------|----------|
| **関数/メソッド行数** | 20行 | 50行 | **100行** |
| **クラス/ファイル行数** | 200行 | 400行 | **1000行** |
| **循環的複雑度** | 5 | 10 | **15** |
| **ネスト深さ** | 2階層 | 3階層 | **4階層** |
| **パラメータ数** | 3個 | 5個 | **7個** |

**太字の「絶対上限」を超える場合、必ずリファクタリングしてください。**

---

## 📝 ドキュメンテーション必須レベル

### Level 1: 必須（違反 = 品質ゲート不合格）
```
✓ すべてのファイルにファイルヘッダー
✓ すべてのパブリッククラス/インターフェースにコメント
✓ すべてのパブリックAPI（関数/メソッド）にコメント
  - パラメータ（@param / Args）
  - 戻り値（@returns / Returns）
  - 例外（@throws / Raises）
```

### Level 2: 強く推奨（コードレビューで指摘）
```
✓ 複雑なロジック（循環的複雑度10以上）
✓ ビジネスルール・制約を反映した実装
✓ 非自明な実装（パフォーマンス最適化、技術的回避策）
```

### Level 3: 任意
```
- 単純なゲッター/セッター
- 自己説明的なコード
```

---

## 🗂️ 参照ドキュメント（パス）

### 共通原則（全言語必須）
```
/01-coding-standards/00-general-principles.md
```

### 言語別規約
```
TypeScript/JS: /01-coding-standards/typescript/  (10ファイル、AI-QUICK-REFERENCE.md参照)
Python:        /01-coding-standards/python/      (16ファイル、AI-QUICK-REFERENCE.md参照)
Java:          /01-coding-standards/java/        (10ファイル、AI-QUICK-REFERENCE.md参照)
SQL:           /01-coding-standards/sql/         (10ファイル)
CSS/SCSS:      /01-coding-standards/css/         (17ファイル)
```

---

## 📋 言語別ドキュメンテーション形式

### TypeScript/JavaScript（JSDoc）
```typescript
/**
 * @fileoverview ファイルの目的
 * @module モジュール名
 */

/**
 * クラス/関数の目的
 * @param name - パラメータの説明
 * @returns 戻り値の説明
 * @throws {ErrorType} 例外の説明
 */
```

### Python（Google Style Docstring）
```python
"""モジュールの目的"""

def function(name: str) -> None:
    """関数の目的
    
    Args:
        name: パラメータの説明
    
    Returns:
        戻り値の説明
    
    Raises:
        ErrorType: 例外の説明
    """
```

### Java（Javadoc）
```java
/**
 * クラス/メソッドの目的
 * @param name パラメータの説明
 * @return 戻り値の説明
 * @throws ExceptionType 例外の説明
 */
```

### SQL（コメント）
```sql
-- ============================================================================
-- Script: ファイル名.sql
-- Description: スクリプトの目的
-- ============================================================================

-- テーブルの目的
CREATE TABLE users (
    -- カラムの説明
    user_id VARCHAR(36) NOT NULL
);
```

### CSS/SCSS（ブロックコメント）
```scss
/**
 * コンポーネントの目的
 * 
 * @component コンポーネント名
 * @responsive レスポンシブ対応状況
 */
```

---

## ✅ コード生成後のセルフチェック

### 構造品質
```
□ 関数は50行以内？
□ クラスは400行以内？
□ 循環的複雑度は10以下？
□ ネストは3階層以内？
□ パラメータは5個以内？
```

### ドキュメンテーション
```
□ ファイルヘッダーがある？
□ パブリッククラスにコメントがある？
□ パブリックAPIにコメントがある？
□ @param、@returns、@throws が完備？
□ 「なぜ」を説明している？（「何を」だけでない）
```

### 自動チェック
```
□ ESLint / Pylint / CheckStyle / Stylelint でエラーなし？
□ ドキュメント生成ツールでエラーなし？
```

---

## 🚨 即座にリファクタリングすべき条件

以下に該当する場合、**必ず**リファクタリングを提案してください：

```
❌ 循環的複雑度が15を超える
❌ 関数が100行を超える
❌ クラスが1000行を超える
❌ ネストが4階層を超える
❌ パラメータが7個を超える
❌ 同じコードが3箇所以上に重複
```

---

## 💡 推奨されるコード構造

### ✅ Good（推奨）
```python
def authenticate_user(email: str, password: str) -> Optional[User]:
    """ユーザーを認証する
    
    メールとパスワードを検証し、認証に成功した場合はUserを返す。
    パスワードはbcryptでハッシュ化して検証される。
    
    Args:
        email: メールアドレス（形式検証済み）
        password: パスワード（平文）
    
    Returns:
        認証成功時はUser、失敗時はNone
    
    Raises:
        ValidationError: メール形式が不正
    """
    # Early Return パターンで複雑度削減
    if not is_valid_email(email):
        raise ValidationError("Invalid email format")
    
    user = find_user_by_email(email)
    if user is None:
        return None
    
    if not verify_password(password, user.password_hash):
        return None
    
    return user
```

### ❌ Bad（非推奨）
```python
# ❌ コメントなし、ネスト深い、複雑度高い
def auth(e, p):
    if e:
        if validate(e):
            u = get(e)
            if u:
                if check(p, u.p):
                    if u.active:
                        return u
    return None
```

---

## 🔧 自動チェックコマンド

### TypeScript/JavaScript
```bash
npx eslint --ext .ts,.tsx src/
npx typedoc --out docs src
```

### Python
```bash
pylint src/
pydocstyle src/
cd docs && make html
```

### Java
```bash
mvn checkstyle:check
mvn javadoc:javadoc
```

### CSS/SCSS
```bash
npx stylelint "src/**/*.{css,scss}"
```

---

## 🎯 コード生成の基本フロー

```
1. 言語を特定
   ↓
2. 共通原則 + 言語別規約を参照
   ↓
3. 数値基準とドキュメンテーション形式を確認
   ↓
4. コード生成
   ↓
5. セルフチェック実行
   ↓
6. 基準超過があればリファクタリング
   ↓
7. 自動チェックツール実行
   ↓
8. 完了
```

---

## 📞 詳細情報

より詳しい情報は以下を参照：
- **詳細指示**: `AI-CODING-INSTRUCTIONS.md`
- **各言語規約**: 上記パスの規約ファイル

---

**重要**: このクイックリファレンスは最小限の情報のみを記載しています。詳細は各規約ファイルを必ず参照してください。
